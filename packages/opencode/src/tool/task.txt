Launch a new agent to handle complex, multistep tasks autonomously. Specify a subagent_type to select which agent. Launch multiple agents concurrently in a single message; each invocation starts fresh unless you reuse a task_id. Agent output is not visible to the user, so summarize results. Tell the agent whether to write code or research, and how to verify its work.