${intro}

OS: ${os}, Shell: ${shell}

${workdirSection}

Use `${tmp}` for temporary work outside the workspace (pre-created and pre-approved).

This tool is for terminal operations (git, build, test, etc.). For file operations use the dedicated tools: Read, Edit, Write, Glob, Grep.

${commandSection}
