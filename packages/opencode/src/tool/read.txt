Read a file or directory from the local filesystem using an absolute path. Returns up to 2000 lines from the start, with each line prefixed `<line>: <content>`; use offset to read later sections. Reads images and PDFs as attachments. Multiple reads can be called in parallel.